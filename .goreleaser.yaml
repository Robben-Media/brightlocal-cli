version: 2

project_name: brightlocal-cli

changelog:
  disable: true

builds:
  - id: brightlocal
    main: ./cmd/brightlocal
    binary: brightlocal-cli
    ldflags: >-
      -s -w
      -X github.com/builtbyrobben/brightlocal-cli/internal/cmd.version={{ .Tag }}
      -X github.com/builtbyrobben/brightlocal-cli/internal/cmd.commit={{ .ShortCommit }}
      -X github.com/builtbyrobben/brightlocal-cli/internal/cmd.date={{ .Date }}
    env:
      - CGO_ENABLED=0
    targets:
      - linux_amd64
      - linux_arm64
      - windows_amd64
      - windows_arm64
  - id: brightlocal_darwin
    main: ./cmd/brightlocal
    binary: brightlocal-cli
    ldflags: >-
      -s -w
      -X github.com/builtbyrobben/brightlocal-cli/internal/cmd.version={{ .Tag }}
      -X github.com/builtbyrobben/brightlocal-cli/internal/cmd.commit={{ .ShortCommit }}
      -X github.com/builtbyrobben/brightlocal-cli/internal/cmd.date={{ .Date }}
    env:
      - CGO_ENABLED=1
    targets:
      - darwin_amd64
      - darwin_arm64

archives:
  - builds:
      - brightlocal
      - brightlocal_darwin
    format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: checksums.txt
